# กล่องคำถามฮิลใจถึงผู้สร้าง 🐢💛 (GitHub Pages + Firebase)

เว็บน่ารักๆ ให้คนส่งคำถาม/ข้อความฮิลใจมา “ถึงผู้สร้าง” แล้วผู้สร้างสามารถเข้ามาตอบกลับได้ผ่านหน้าเว็บ (โหมดแอดมิน)

> ทำบน GitHub Pages ได้ ✅ แต่จำเป็นต้องมี backend เก็บข้อความ (เพราะ GitHub Pages เป็นเว็บ static)  
> โปรเจกต์นี้ใช้ **Firebase Authentication + Cloud Firestore** เป็น backend

---

## 1) สร้าง Firebase Project

1. เข้า Firebase Console → Create project
2. สร้าง **Cloud Firestore** (เลือกโหมด Production หรือ Test ก็ได้ แต่สุดท้ายให้ตั้ง Rules ตามด้านล่าง)
3. เปิด **Authentication**
   - เปิด Sign-in method: ✅ Anonymous, ✅ Email/Password
4. สร้างผู้ใช้สำหรับ “ผู้สร้าง” (Email/Password)
   - ไปที่ Authentication → Users → Add user
   - **คัดลอก UID ของผู้สร้าง** ไว้ (จะใช้ใน Firestore Rules)

---

## 2) ตั้ง Authorized domains (สำคัญมาก)

ไปที่ Firebase → Authentication → Settings → Authorized domains  
เพิ่มโดเมน GitHub Pages ของคุณ เช่น

- `yourname.github.io`
- หรือถ้าใช้ repo pages: `yourname.github.io/yourrepo` (ให้ใส่แค่โดเมน `yourname.github.io` ก็พอ)

---

## 3) ตั้ง Firestore Security Rules

เปิด Firestore → Rules แล้วแทนด้วยไฟล์ `firestore.rules` ในโปรเจกต์นี้  
จากนั้น **แก้ `ADMIN_UID_HERE`** ให้เป็น UID ของผู้สร้างจริง

---

## 4) ใส่ Firebase config ในไฟล์ `firebase-config.js`

เปิดไฟล์ `firebase-config.js` แล้ววางค่า config จาก

Firebase console → Project settings → Your apps → Web app → Firebase SDK snippet (config)

---

## 5) อัปโหลดขึ้น GitHub และเปิด GitHub Pages

1. สร้าง repo ใหม่ใน GitHub (Public/Private ก็ได้ แต่ถ้า Private บางคนจะเข้ายาก)
2. อัปโหลดไฟล์ทั้งหมดในโฟลเดอร์นี้ขึ้น repo:
   - `index.html`
   - `styles.css`
   - `app.js`
   - `firebase-config.js`
   - `firestore.rules` (เก็บไว้เป็นอ้างอิงก็ได้)
3. เปิด GitHub Pages:
   - Settings → Pages
   - Source: Deploy from a branch
   - Branch: `main` / `/root`
4. รอ GitHub สร้างลิงก์เว็บ แล้วกดเข้าใช้งานได้เลย 🎉

---

## การใช้งาน

### ฝั่งคนส่งข้อความ (Visitor)
- พิมพ์ชื่อของคุณ
- พิมพ์คำถาม/ข้อความ
- กดส่ง (Enter เพื่อส่ง, Shift+Enter ขึ้นบรรทัดใหม่)

### ฝั่งผู้สร้าง (Admin)
- กด “สำหรับผู้สร้าง 🔐”
- ใส่อีเมล/รหัสผ่าน Firebase (Email/Password)
- จะเห็นรายการห้องแชท และตอบกลับได้

---

## ปรับแต่ง
- เปลี่ยนชื่อผู้สร้าง/ข้อความ: ดู `firebase-config.js` และ `index.html`
- ปรับสี/ธีม: `styles.css`

---

## หมายเหตุเรื่องความปลอดภัย
- Firebase config ไม่ใช่ความลับ แต่ **Firestore Rules** คือหัวใจ
- โปรเจกต์นี้จำกัดการเข้าถึง: Visitor อ่านได้เฉพาะห้องของตัวเอง และผู้สร้างอ่านได้ทุกห้อง (ตาม Rules)

ขอให้เป็นเว็บฮิลใจที่น่ารักมาก ๆ นะ 💛✨
